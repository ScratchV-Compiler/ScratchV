# ScratchV 窥孔优化器技术设计文档

> **文档版本**：v1.1
> **创建日期**：2026-07-19
> **修订日期**：2026-08-01
> **作者**：袁森
> **课题名称**：窥孔优化器
> **涉及模块**：`asm_peephole.py`、`_asm_parser.py`、`compiler.py`、`main.py`
> **功能范围**：ScratchV 原生 RV32IM 汇编解析、局部指令匹配与替换、固定点迭代、优化统计及语义安全增强

---

## 一、功能介绍

### 1.1 功能概述

窥孔优化器位于 ScratchV 原生 RISC-V 后端的汇编后处理阶段。它使用较小的滑动窗口扫描同一基本块内的相邻指令，识别低效或冗余的指令组合，并在能够证明语义等价时进行替换。

例如：

```asm
addi t0, t1, 3
addi t0, t0, 5
```

可以优化为：

```asm
addi t0, t1, 8
```

本课题的重点不仅是增加规则，还包括修正现有规则、统一汇编解析、补齐安全边界、改进测试和建立可解释的统计口径。

### 1.2 设计目标

- **语义等价**：优化前后程序的寄存器、控制流、可观察内存和输出保持一致。
- **保守优化**：任何操作数、边界或活跃性条件无法确认时，保留原始汇编。
- **规则可扩展**：规则通过操作数谓词和替换回调表达，不在核心扫描流程中按规则名硬编码。
- **文本保真**：未参与转换的行按 `raw` 原样输出，标签、directive、注释和未知内容不得意外丢失。
- **结果可统计**：区分规则命中次数、成功转换次数、有效指令减少量、迭代轮数和终止原因。
- **编译器可集成**：保持现有 Python API 和命令行参数，功能开关继续默认关闭。

### 1.3 当前实现与目标状态

以下状态以 2026-08-02 的项目工作区为基线。文档中的“当前”表示已经存在的实现，“目标”表示本课题计划完成但尚未实现的内容。

| 能力 | 当前状态 | 目标状态 |
|---|---|---|
| 默认规则 | 共五条 | 修正五条现有规则，并新增三条规则，共八条 |
| 两条 `mv` 规则 | 已实现但约束错误：成对规则实际只比较两条指令的目标寄存器；中间移动规则被前一规则遮蔽 | 按本设计的精确寄存器关系和活跃性条件重写 |
| 汇编解析 | `asm_peephole.py` 自带 `AsmLine` 和解析函数 | 统一使用 `_asm_parser.py` 的 `ParsedAsmLine` |
| 基本块和活跃性 | 尚未实现 | 只做基本块内的保守前向检查 |
| 三条恒等运算规则 | 尚未实现 | 新增 `mv rd,rd`、`addi rd,rd,0`、`slli rd,rd,0` 删除 |
| 统计 | 只有 `total_matches` 和总变更次数 | 新增 `PeepholeStats`，同时保持旧 API 兼容 |
| 测试 | 当前 17 项定向测试能够通过，但两个 `mv` 测试只断言 `changes >= 0` | 精确验证完整输出、命中数、负例和边界 |
| 编译器入口 | `--peephole-asm` 已接入且默认关闭 | 仅允许 ScratchV 原生 RV32IM 汇编路径使用 |

### 1.4 范围约束

本课题只优化 ScratchV 原生后端生成的 RV32IM 汇编，不处理 LLVM IR 或 LLVM RV64FD 输出；不进行跨基本块、跨函数或全程序数据流分析，也不替代 IR 优化、寄存器分配和指令调度。

---

## 二、优化规则设计

### 2.1 目标规则表示

目标规则由名称、操作码模式、操作数数量、安全谓词和替换回调组成：

```text
rule ::= name + pattern + operand_counts + predicate + transform
```

示意数据结构：

```python
@dataclass(frozen=True)
class PeepholeRule:
    name: str
    pattern: tuple[str, ...]
    operand_counts: tuple[int, ...]
    predicate: MatchPredicate
    transform: TransformCallback
```

- `predicate` 只检查窗口并返回绑定结果，不修改输入。
- `transform` 仅在 `predicate` 成功后执行，生成零条或多条 `ParsedAsmLine`。
- 操作数数量必须精确匹配，不能用缺省寄存器、立即数或标签补全错误输入。
- 新规则通过注册 `PeepholeRule` 加入默认规则列表，核心扫描流程不得根据 `rule.name` 分支。
- 所有寄存器比较都先进行 ABI 名称与 `x0`～`x31` 的规范化。

示例：

```python
PeepholeRule(
    name="addi+addi fusion",
    pattern=("addi", "addi"),
    operand_counts=(3, 3),
    predicate=match_addi_addi,
    transform=fuse_addi_addi,
)
```

### 2.2 五条现有规则及修正目标

#### 规则一：连续立即数加法合并（`addi+addi fusion`）

```asm
addi rd, rs, imm1
addi rd, rd, imm2
```

目标替换为：

```asm
addi rd, rs, imm1 + imm2
```

安全条件：

- 两条指令都恰有三个操作数；
- 第一条目标寄存器等于第二条目标和源寄存器；
- 两个立即数均可解析为整数，且 `imm1`、`imm2` 各自位于 `[-2048, 2047]`；
- 合并结果位于 `addi` 的有符号 12 位范围 `[-2048, 2047]`。

当前实现没有范围检查，解析失败时还可能生成括号表达式，因此属于“已实现但需修正”。

#### 规则二：回拷贝消除（`redundant mv pair elimination`）

```asm
mv a, b
mv b, a
```

执行第一条后，`a` 和 `b` 都等于原来的 `b`；第二条不会再改变状态。因此正确替换是：

```asm
mv a, b
```

该转换只删除第二条指令，不需要活跃性分析，但必须要求 `a` 不是零寄存器。RISC-V 对零寄存器的写入会被丢弃；例如 `mv x0,t0; mv t0,x0` 会把 `t0` 清零，只保留第一条将改变语义。安全条件因此是：两条指令各有两个操作数，规范化后满足精确交叉关系，且 `a != x0`。

当前实现实际只检查两条 `mv` 的目标寄存器相同，可能把以下序列整对误删：

```asm
mv t0, t1
mv t0, t2
```

因此必须先用失败测试固定该缺陷，再重写约束；文档不将当前行为视为正确实现。

#### 规则三：常量加载与加法合并（`li+addi fusion`）

```asm
li rd, imm1
addi rd, rd, imm2
```

目标替换为：

```asm
li rd, folded_imm
```

安全条件：

- 操作数数量分别为两个和三个；
- 两条指令的目标寄存器以及第二条源寄存器相同；
- `imm1` 和 `imm2` 都是支持的整数字面量；
- `imm2` 满足 `addi` 的有符号 12 位范围；
- 按 RV32 的 32 位回绕语义计算 `folded_imm`，并输出规范化的 32 位常量。

符号、重定位表达式或无法确认的常量形式不得折叠。

#### 规则四：恒真分支简化（`beq zero-zero to jump`）

```asm
beq x0, x0, label
```

目标替换为：

```asm
j label
```

安全条件：

- `beq` 恰有三个操作数；
- 前两个寄存器规范化后均为零寄存器；
- 第三个操作数是存在且可保留的控制流目标。

缺少目标的 `beq x0,x0` 必须原样保留，禁止生成臆造的 `j L0`。

#### 规则五：中间寄存器移动消除（`redundant mv elimination`）

```asm
mv tmp, src
mv dst, tmp
```

目标替换为：

```asm
mv dst, src
```

该替换会取消对 `tmp` 的写入，只有 `tmp` 不是零寄存器且能够证明该值在当前基本块内已经死亡时才安全。`mv x0,a0; mv a1,x0` 的原结果是 `a1=0`，不能改成 `mv a1,a0`。本课题采用保守的块内判定：

- 从窗口后向前扫描后续指令；
- 若第一次访问 `tmp` 包含读取，则不得优化；读改写指令同时属于 `uses` 和 `defs`，必须按读取处理；
- 只有第一次访问是纯覆写，即 `tmp` 属于 `defs` 且不属于 `uses`，才可以优化；
- 若先遇到标签、控制流、调用、返回、未知指令或直接到达基本块末尾，则活跃性未知，不优化。

当前实现的寄存器约束不能匹配文档示例，并且会被规则二的相同操作码模式遮蔽，属于“已实现但不可依赖”。

### 2.3 三条计划新增规则

#### 规则六：自移动删除（`self mv elimination`）

```asm
mv rd, rd
```

源、目标寄存器规范化后相同时，替换为空。`mv t0,x5` 也应识别为同一寄存器；操作数数量不为两个时不匹配。

#### 规则七：加零删除（`addi zero elimination`）

```asm
addi rd, rd, 0
```

源、目标寄存器相同且立即数规范化后严格为零时，替换为空。`addi t0,t1,0` 是一次复制，不能删除。

#### 规则八：零位左移删除（`slli zero elimination`）

```asm
slli rd, rd, 0
```

源、目标寄存器相同且移位量严格为零时，替换为空。`slli t0,t1,0` 和 `slli t0,t0,1` 均不得匹配。

### 2.4 八条目标规则汇总

| 序号 | 规则名称 | 窗口 | 输出指令数 | 是否需要活跃性 |
|---|---|---:|---:|---|
| 1 | `addi+addi fusion` | 2 | 1 | 否 |
| 2 | `redundant mv pair elimination` | 2 | 1 | 否 |
| 3 | `li+addi fusion` | 2 | 1 | 否 |
| 4 | `beq zero-zero to jump` | 1 | 1 | 否 |
| 5 | `redundant mv elimination` | 2 | 1 | 是，块内保守判定 |
| 6 | `self mv elimination` | 1 | 0 | 否 |
| 7 | `addi zero elimination` | 1 | 0 | 否 |
| 8 | `slli zero elimination` | 1 | 0 | 否 |

### 2.5 通用安全约束

- 不跨越标签、directive、空行、纯注释、分支、跳转、调用、返回和未知指令匹配。
- 带行内标签的指令不参与删除或合并，避免替换时丢失控制流入口。
- 不删除仍可能被后续读取的寄存器定义。
- 不改变访存、函数调用或其他有副作用指令的顺序。
- 操作数数量、立即数和寄存器关系必须全部验证成功。
- 无法证明安全时保留原始指令，不把“可能正确”当作可优化条件。

---

## 三、目标总体结构

当前实现已经具有滑动窗口和固定点循环，但解析、匹配、替换和统计仍耦合在 `asm_peephole.py` 中。目标结构如下：

```text
原始 RV32IM 汇编文本
    ↓
_asm_parser.py → ParsedAsmLine 列表
    ↓
识别基本块边界和不可跨越屏障
    ↓
规则窗口 + operand_counts
    ↓
predicate：操作数、寄存器、立即数、活跃性检查
    ↓
transform：生成等价替换
    ↓
固定点迭代与收敛检查
    ↓
原始文本保真输出 + PeepholeStats
```

### 3.1 公共汇编解析

优化器统一使用 `_asm_parser.py` 提供的 `ParsedAsmLine`，至少保留：

```python
ParsedAsmLine(
    raw="loop: addi t0, t0, 1  # increment",
    label="loop",
    opcode="addi",
    operands=["t0", "t0", "1"],
    comment="increment",
    lineno=10,
)
```

要求：

- 区分空行、纯注释、标签、directive、普通指令以及标签与指令同一行；
- 未修改行直接输出 `raw`，不因解析往返而重新格式化；
- 解析失败的行保留 `raw`，并作为窗口屏障；
- 有效指令计数只统计具有真实操作码的指令，不统计标签、directive、注释和空行。

### 3.2 基本块与窗口边界

以下内容终止当前窗口，规则不得跨越：

- 独立标签或行内标签；
- 条件分支和无条件跳转；
- `call`、`ret` 及其他改变控制流的指令；
- `.text`、`.section`、`.loc`、`.cfi_*` 等 directive；
- 纯注释、空行和无法分类的指令。

单指令规则遇到行内标签时同样不执行。例如 `exit: mv a0,a0` 保持原样，不能在删除 `mv` 时一起删除 `exit`。

### 3.3 滑动窗口与规则调用

对每条规则依次执行：

1. 检查剩余行数和操作码模式；
2. 检查每条指令的精确操作数数量；
3. 拒绝包含边界或不透明内容的窗口；
4. 调用规则 `predicate` 得到寄存器、立即数等绑定；
5. 必要时调用块内活跃性检查；
6. 调用 `transform` 生成替换；
7. 仅在序列实际改变时记录一次成功转换。

未修改窗口继续使用原始 `raw`；对发生转换的窗口，原有注释按出现顺序保留。若指令被完全删除，其注释转换为独立注释行。

### 3.4 块内轻量级活跃性

活跃性仅用于规则五，不扩大为全局数据流分析。对候选 `tmp`，从窗口后一条指令开始扫描：

- 第一次访问包含读取：值仍活跃，返回“不安全”；同一条指令同时读取和写入时按读取优先；
- 第一次访问是纯覆写，即 `tmp in defs` 且 `tmp not in uses`：旧值已死，返回“安全”；
- 到达屏障或块尾仍未访问：live-out 未知，返回“不安全”；
- `call`、未知指令和无法准确分类的伪指令：保守视为可能读取。

安全正例必须在块内显式证明旧值死亡：

```asm
mv t0, a0
mv a1, t0
li t0, 0
ret
```

可替换为：

```asm
mv a1, a0
li t0, 0
ret
```

以下序列到块尾仍无法确认 `t0` 是否 live-out，因此不优化：

```asm
mv t0, a0
mv a1, t0
ret
```

### 3.5 固定点、收敛与统计

八条目标规则都满足单向收敛：七条规则严格减少有效指令数，恒真分支规则将 `beq` 单向规范化为 `j`，且不存在逆向规则。因此正常情况下能够到达固定点。循环规则如下：

- 只有输出序列与输入窗口不同才设置 `changed=True`；
- 相同替换不计入命中和转换次数；
- `iterations` 记录完成的整轮扫描，包括最终无变化扫描；
- 保留 50 轮安全上限；
- 若第 50 轮仍发生变化，则返回当前结果，设置 `converged=False`、`termination_reason="iteration_limit"` 并向 CLI/编译器报告警告；若第 50 轮无变化，则设置为正常固定点。

目标统计结构：

```python
PeepholeStats(
    input_instructions=120,
    output_instructions=108,
    transformations=9,
    instructions_removed=12,
    per_rule={"addi+addi fusion": 4, ...},
    iterations=3,
    converged=True,
    termination_reason="fixed_point",
)
```

`transformations` 是成功替换次数，`instructions_removed` 是输入、输出有效指令数之差，两者不要求相等。

### 3.6 编译流程和接口

- 仅在 ScratchV 原生 RV32IM 汇编路径运行窥孔优化器。
- `--backend llvm --peephole-asm` 等不兼容组合必须在 `main.py` 或 `compiler.py` 中拒绝或明确跳过。
- 汇编后处理顺序保持为：窥孔优化 → 常量加载合并 → 指令调度 → 美化 → 计数。
- 保持 `optimize(text) -> tuple[str, int]`、`total_matches` 和 `report()` 兼容。
- 新增只读 `last_stats` 暴露详细统计。
- `python -m scratchv.backend.asm_peephole --list-rules` 不要求输入文件，且不得因包级提前导入产生 `RuntimeWarning`。

---

## 四、正确性与异常处理设计

### 4.1 整数字面量和 RV32 语义

统一整数解析器支持：

- 带可选正负号的十进制；
- `0x` 十六进制、`0b` 二进制和 `0o` 八进制；
- 不支持符号表达式、重定位表达式和无法确定值的宏。

`addi+addi` 只有在合并结果位于 `[-2048, 2047]` 时才生成新 `addi`。`li+addi` 按 RV32 模 `2^32` 计算，再输出规范化的 32 位常量；任何输入或结果无法准确表示时保留原序列。

### 4.2 标签、注释和未知内容

- 带标签的候选指令保持原样，不迁移标签。
- directive、空行、纯注释和未知内容按 `raw` 原样保留，并阻断窗口。
- 合并两条带注释的指令时，注释按原顺序保留。
- 删除带注释的恒等指令时，保留为独立注释行。
- 不得因为 `strip()`、重新分词或重新序列化而丢失文件首尾空行和未修改行格式。

### 4.3 寄存器规范化

建立完整的 RV32 整数寄存器别名映射，例如：

```text
zero ↔ x0
ra   ↔ x1
sp   ↔ x2
t0   ↔ x5
a0   ↔ x10
```

规则匹配使用规范化名称，输出优先沿用输入中的原始写法。无法识别的寄存器名称不参加需要别名判断的规则。

### 4.4 CLI 文件安全

- 文件统一按 UTF-8 读取和写入；
- 输入不存在、编码错误或写入失败时输出明确错误并返回非零退出码；
- 输入与输出为同一路径时，先写同目录临时文件，成功后原子替换；
- 优化或写入失败不得截断、覆盖原文件。

### 4.5 保守优化原则

窥孔优化的首要目标是语义正确，其次才是减少指令。无法证明安全的候选窗口应保持不变，并且不计入规则命中。

---

## 五、修改模块与实现步骤

### 5.1 涉及文件

| 文件 | 计划修改 |
|---|---|
| `scratchv/backend/asm_peephole.py` | 使用公共解析器；重写规则谓词和替换；增加活跃性、统计与安全检查 |
| `scratchv/backend/_asm_parser.py` | 完善 `ParsedAsmLine`、寄存器别名、def-use 和屏障分类 |
| `scratchv/backend/__init__.py` | 避免 `python -m` 执行目标模块前被包级导入 |
| `scratchv/compiler.py` | 仅在 RV32IM 汇编路径调用优化器，并输出详细统计 |
| `scratchv/main.py` | 校验 `--peephole-asm` 与后端组合 |
| `tests/test_asm_peephole.py` | 增加八条规则的精确测试、安全回归、CLI 和集成测试 |
| `benchmarks/bench_asm_peephole.py` | 按有效指令统计，覆盖八条规则并记录迭代和逐规则命中 |
| `docs/topics/13-窥孔优化器.md` | 实现完成后同步规则语义、CLI 和课题状态 |

### 5.2 实现顺序

1. 为已确认的两条 `mv` 约束错误、行内标签丢失、缺少分支目标、非法/超范围立即数和空转 50 轮补充失败测试。
2. 统一到 `_asm_parser.py`，建立 `ParsedAsmLine`、寄存器别名、有效指令计数和屏障分类。
3. 将 `PeepholeRule` 改为 `operand_counts + predicate + transform`，移除按规则名分支。
4. 修正五条现有规则，并按顺序加入三条新增规则。
5. 增加规则五所需的块内保守活跃性判断。
6. 增加 `PeepholeStats`、正确的固定点判定和未收敛报告。
7. 完成编译器后端保护、CLI 原子写入和模块导入兼容。
8. 运行定向测试、全量回归、RV32IM 语义验证和基准测试，最后同步课题文档。

### 5.3 使用方式

```bash
scratchv model.onnx -o output.s --peephole-asm
python -m scratchv.backend.asm_peephole input.s -o optimized.s --report
python -m scratchv.backend.asm_peephole --list-rules
```

---

## 六、测试设计

### 6.1 当前测试基线

当前 `tests/test_asm_peephole.py` 共 17 项测试，能够通过，但不能作为正确性完成证明：

- 两个 `mv` 测试只断言 `changes >= 0`，即使规则完全不生效也会通过；
- 多个正例仅检查结果中存在某个子串，没有比较完整输出；
- 缺少操作数数量、行内标签、立即数范围、活跃性、幂等、CLI 和编译器集成测试。

修改后每个规则测试都必须比较完整输出、精确 `changes`、对应的 `total_matches` 和 `last_stats`。

### 6.2 八条规则的核心用例

| 规则 | 正例 | 必要反例 |
|---|---|---|
| `addi+addi fusion` | `addi t0,t1,3; addi t0,t0,5` → `addi t0,t1,8` | 任一输入立即数为 `2048`、合并结果越界、寄存器关系错误、非法立即数 |
| `redundant mv pair elimination` | `mv t0,t1; mv t1,t0` → 保留第一条 | `mv t0,t1; mv t0,t2` 和 `mv x0,t0; mv t0,x0` 必须原样保留 |
| `li+addi fusion` | `li a0,10; addi a0,a0,5` → `li a0,15` | 符号常量、寄存器关系错误、RV32 无法表示 |
| `beq zero-zero to jump` | `beq x0,zero,exit` → `j exit` | 缺少目标、普通寄存器比较 |
| `redundant mv elimination` | 后续先纯覆写 `tmp` 时合并 | 后续先读取、先读改写、直接到块尾或 `tmp=x0` |
| `self mv elimination` | `mv t0,x5` 删除 | 两个不同寄存器或操作数缺失 |
| `addi zero elimination` | `addi t0,x5,0` 删除 | `addi t0,t1,0`、立即数非零 |
| `slli zero elimination` | `slli t0,x5,0` 删除 | `slli t0,t1,0`、移位量非零 |

### 6.3 边界和回归测试

- 带行内标签的候选指令保持原样，标签不得丢失；
- 不跨越独立标签、directive、注释、空行和控制流边界；
- `beq x0,x0` 不得生成默认目标；
- 覆盖 `-2048`、`2047`、任一输入为 `2048`、合并结果越界、负数和十六进制立即数；
- 无法解析的立即数、未知指令和不完整操作数保持原样；
- 未修改行、文件首尾空行和注释保持完整；
- 同形替换不计数，优化器重复运行结果不变；
- 自定义规则第 50 轮仍变化时报告未收敛，第 50 轮无变化时报告正常固定点；
- 两条 `mv` 规则在默认顺序下均可到达，且不会互相误匹配。

### 6.4 CLI、编译器和语义验证

- `--list-rules` 无输入文件即可列出八条规则，且无 `RuntimeWarning`；
- CLI 输入输出同路径、读取失败和写入失败均通过测试；
- ScratchV 原生 RV32IM 后端能够启用优化；
- LLVM 后端组合被拒绝或安全跳过；
- 至少选择三组 ScratchV 生成的完整 RV32IM 用例，使用项目 RV32 模拟器或现有 TinyFive 环境比较优化前后的返回值、输出寄存器和可观察内存。

---

## 七、性能与效果验证

### 7.1 统计口径

| 指标 | 精确定义 |
|---|---|
| `transformations` | 实际改变输出的规则替换次数，等于 `per_rule` 之和 |
| `per_rule` | 每条规则实际生效的次数 |
| `input_instructions` | 输入中具有真实操作码的有效指令数 |
| `output_instructions` | 输出中具有真实操作码的有效指令数 |
| `instructions_removed` | `input_instructions - output_instructions` |
| `iterations` | 完成的整轮扫描数，包含最终无变化扫描 |
| `converged` | 最后一轮扫描是否无变化；第 50 轮无变化也为 `true` |
| `termination_reason` | `fixed_point` 或 `iteration_limit` |
| 优化耗时 | 解析、匹配、替换和输出的总耗时 |

标签、directive、空行、注释和文件尾换行均不计入有效指令。一次 2→1 转换减少一条指令，一次 1→0 转换也减少一条指令；`beq` → `j` 为 1→1 转换，指令减少量为零。因此“转换次数”和“减少量”必须分别统计。

### 7.2 基准测试

当前 `benchmarks/bench_asm_peephole.py` 使用换行数计算减少量，且合成数据主要覆盖 `addi+addi`。目标基准应：

- 使用 100、500、1000、2000 和 5000 条有效指令；
- 分别生成八条规则的可优化模式和不可优化反例；
- 固定随机种子、重复次数和运行环境；
- 记录逐规则命中、迭代轮数、有效指令减少量和耗时分布；
- 加入 ScratchV 实际生成的 RV32IM 汇编；
- 报告是否触发迭代上限，不把尾换行变化计为优化效果。

---

## 八、预期成果与验收

- 五条现有规则的真实缺陷被测试覆盖并修正；
- 三条恒等运算删除规则加入默认规则列表，总规则数为八条；
- 优化器统一复用 `ParsedAsmLine`，未修改文本保持原样；
- 规则通过谓词和替换回调扩展，不再按规则名硬编码；
- 标签、操作数、立即数、控制流和活跃性边界得到保守处理；
- `PeepholeStats` 提供明确、可验证的统计口径；
- 编译器、独立 CLI 和 Python API 保持兼容，非 RV32IM 后端不会误用优化器；
- 定向测试、全量回归、RV32IM 语义验证和有效指令基准全部完成；
- 代码注释、开发文档和 `docs/topics/13-窥孔优化器.md` 与最终实现同步。

---

## 九、附录

### 9.1 完整优化示例

优化前：

```asm
main:
    li t0, 5
    addi t0, t0, 3
    addi t0, t0, 2
    beq x0, x0, exit
exit:
    mv a0, t0
    mv a0, a0
    addi a0, a0, 0
    slli a0, a0, 0
    ret
```

达到固定点后：

```asm
main:
    li t0, 10
    j exit
exit:
    mv a0, t0
    ret
```

该示例依次触发 `li+addi fusion`、`beq zero-zero to jump` 和三条计划新增的恒等运算删除规则。`exit:` 是独立标签，因此不会随其后的指令删除而丢失。

### 9.2 两条 `mv` 规则对比

回拷贝消除无需活跃性：

```asm
mv t0, t1
mv t1, t0
```

替换为：

```asm
mv t0, t1
```

中间寄存器消除必须在块内证明旧值死亡：

```asm
mv t0, a0
mv a1, t0
li t0, 0
```

替换为：

```asm
mv a1, a0
li t0, 0
```

### 9.3 参考资料

- [`窥孔优化器开发文档.md`](./窥孔优化器开发文档.md)
- `docs/topics/13-窥孔优化器.md`
- `scratchv/backend/asm_peephole.py`
- `scratchv/backend/_asm_parser.py`
- `scratchv/compiler.py`
- `scratchv/main.py`
- `tests/test_asm_peephole.py`
- `benchmarks/bench_asm_peephole.py`
- RISC-V Instruction Set Manual
