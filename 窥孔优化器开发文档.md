# ScratchV 汇编级窥孔优化器开发文档

> **文档版本**：v1.1
> **创建日期**：2026-08-01
> **修订日期**：2026-08-02
> **作者**：袁森
> **课题名称**：窥孔优化器(topic 13)
> **涉及模块**：`asm_peephole.py`、`_asm_parser.py`、`compiler.py`、`main.py`

---

## 1. 功能概述与目标

### 1.1 背景与动机

- **现状问题**：ScratchV 生成 RISC-V 汇编后可能留下连续立即数运算、恒真分支和冗余寄存器搬运。现有窥孔优化器已接入编译流程，但部分规则缺少严格的操作数、立即数范围、标签边界和寄存器活跃性校验，存在生成无效汇编或改变程序语义的风险。
- **应用场景**：在 ScratchV 原生 RV32IM 代码生成后优化相邻汇编指令；也可通过独立命令行处理汇编文件并查看优化统计。

### 1.2 功能描述

- **一句话定义**：在不改变程序可观察行为的前提下，对 ScratchV 生成的局部 RV32IM 汇编序列进行保守匹配和等价替换。
- **核心价值**：减少冗余指令，并为汇编级优化规则提供统一、可测试的扩展位置。

### 1.3 目标与非目标

| 类型 | 内容 |
|------|------|
| ✅ 包含范围 | 统一汇编解析；修正现有五条规则；新增 `mv rd,rd`、`addi rd,rd,0`、`slli rd,rd,0` 三条规则；校验操作数、立即数、标签和基本块边界；完善固定点迭代、优化统计及必要测试。 |
| ❌ 不包含范围 | 不进行跨基本块、跨函数或全程序数据流优化；不替代 IR 优化、寄存器分配或指令调度；不处理 LLVM IR 或 LLVM RV64FD 输出；本次不默认启用 `--peephole-asm`。 |

### 1.4 当前实现基线与目标状态

以下状态以 2026-08-02 的项目工作区为准，用于区分“当前实现”和“本课题目标”：

| 能力 | 当前状态 | 本课题目标 |
|------|----------|------------|
| 编译器接入、独立 CLI、Python API | 已实现；`--peephole-asm` 默认关闭 | 保持接口兼容，并限制为 ScratchV 原生 RV32IM 汇编后端 |
| 五条默认规则 | 已配置，但两条 `mv` 规则约束错误；部分规则缺少操作数、范围和标签校验 | 修正语义并补齐安全检查 |
| 公共汇编解析 | 优化器仍使用自身的 `AsmLine` 和解析函数 | 统一复用 `_asm_parser.py` 的 `ParsedAsmLine` |
| 基本块、活跃性和详细统计 | 尚未实现 | 增加块内保守分析与 `PeepholeStats` |
| 三条恒等运算删除规则 | 尚未实现 | 新增并加入默认规则列表，总目标规则数为八条 |
| 自动化测试 | 当前定向测试为 17 项且能够通过，但两个 `mv` 测试未断言输出 | 改为精确验证输出、命中数、反例和边界 |

---

## 2. 设计与规格说明

### 2.1 用户视角（外部接口）

- **编译器入口**：`scratchv model.onnx -o output.s --peephole-asm`
- **独立命令行**：
  - `python -m scratchv.backend.asm_peephole input.s -o optimized.s --report`
  - `python -m scratchv.backend.asm_peephole --list-rules`
- **CLI 约束**：`--list-rules` 不要求 `input`，且执行时不得产生模块重复导入的 `RuntimeWarning`；文件按 UTF-8 读写，失败时返回非零退出码。
- **后端约束**：`--peephole-asm` 仅适用于 ScratchV 原生 RV32IM 汇编输出；与 `--backend llvm` 等非 RV32IM 汇编后端组合时应明确拒绝或跳过，不能把 LLVM IR 交给本优化器。
- **Python API**：保持 `optimize(text) -> tuple[str, int]`、`report()` 和 `total_matches` 兼容，并通过只读属性 `last_stats` 提供详细统计。

### 2.2 内部设计（核心逻辑）

- **核心数据结构**：
  - `ParsedAsmLine`：保存标签、操作码、操作数、注释和原始行信息。
  - `PeepholeRule`：至少包含 `name`、`pattern`、`operand_counts`、无副作用的 `predicate` 和 `transform` 回调，使新规则无需在核心流程中按规则名分支。
  - `PeepholeStats`：计划新增，字段包括 `input_instructions`、`output_instructions`、`transformations`、`instructions_removed`、`per_rule`、`iterations`、`converged` 和 `termination_reason`。
- **关键流程**：

  ```text
  汇编文本
      → 使用 _asm_parser.py 结构化解析
      → 识别标签、directive、控制流、调用和未知指令边界
      → 在单个基本块内选择相邻指令窗口
      → 校验操作数数量、寄存器关系和立即数范围
      → 必要时检查块内寄存器活跃性
      → 构造等价替换并保留标签、注释和未知内容
      → 重复扫描至固定点或 50 轮上限
      → 输出优化结果和统计
  ```

- **状态管理**：不新增全局可变状态。每次 `optimize()` 开始时重置统计；`iterations` 包含最终无变化扫描；只有替换后的序列与原窗口不同才计为一次 `transformation`。八条目标规则都满足单向收敛：七条规则严格减少有效指令数，恒真分支规则只进行 `beq` → `j` 的单向规范化，且不存在逆向规则。50 轮仅作为异常安全上限；若第 50 轮仍发生变化，则返回当前结果并报告未收敛警告；若第 50 轮无变化，则正常标记为已到达固定点。

目标规则：

| 规则 | 目标替换 | 必要安全条件 |
|------|----------|--------------|
| `addi+addi fusion` | `addi rd,rs,imm1; addi rd,rd,imm2` → `addi rd,rs,imm1+imm2` | 两行各有 3 个操作数；`imm1`、`imm2` 及其和均位于 `[-2048, 2047]` |
| `redundant mv pair elimination` | `mv a,b; mv b,a` → `mv a,b` | 两行各有 2 个操作数；规范化后满足精确交叉关系；`a` 不是零寄存器；仅删除第二条，不需要活跃性分析 |
| `li+addi fusion` | `li rd,imm1; addi rd,rd,imm2` → `li rd,imm1+imm2` | 操作数分别为 2、3 个；`addi` 立即数合法；按 RV32 语义计算并生成可表示的 32 位常量 |
| `beq zero-zero to jump` | `beq x0,x0,label` → `j label` | 恰有 3 个操作数；前两个为 `x0`/`zero`；目标存在且合法，禁止臆造默认标签 |
| `redundant mv elimination` | `mv tmp,src; mv dst,tmp` → `mv dst,src` | 两行各有 2 个操作数；`tmp` 不是零寄存器；窗口后第一次访问必须是只写不读的块内纯覆写；先读取、读改写或到块尾仍不确定时不优化 |
| `self mv elimination` | 删除 `mv rd,rd` | 恰有 2 个操作数；源、目标规范化后为同一整数寄存器 |
| `addi zero elimination` | 删除 `addi rd,rd,0` | 恰有 3 个操作数；源、目标寄存器等价；立即数规范化后严格为 0 |
| `slli zero elimination` | 删除 `slli rd,rd,0` | 恰有 3 个操作数；源、目标寄存器等价；移位量严格为 0 |

### 2.3 接口定义（模块间交互）

- **上游依赖**：`compiler.py` 只在 ScratchV 原生 RV32IM 汇编路径提供输入；`_asm_parser.py` 提供解析、输出和 def-use 分类；`main.py` 提供 `--peephole-asm` 开关及后端组合校验。
- **下游影响**：后处理顺序保持为窥孔优化 → 常量加载合并 → 指令调度 → 美化 → 计数。必须保持标签、directive、注释、未知内容和 `optimize(text) -> tuple[str, int]` 接口兼容。

---

## 3. 模块修改与实现步骤

### 3.1 涉及的文件清单

| 文件路径 | 修改类型 | 修改内容概述 |
|----------|----------|--------------|
| `scratchv/backend/asm_peephole.py` | 重构/修复 | 复用公共解析器，修正规则，增加边界、活跃性、范围校验和统计 |
| `scratchv/backend/_asm_parser.py` | 增强 | 完善控制流、调用和未知指令的 def-use/屏障分类 |
| `scratchv/backend/__init__.py` | 兼容性调整 | 避免执行 `python -m scratchv.backend.asm_peephole` 时提前导入同一模块 |
| `scratchv/compiler.py` | 修改 | 仅在 RV32IM 汇编路径运行优化器，并输出变更次数和有效指令减少量 |
| `scratchv/main.py` | 修改 | 校验 `--peephole-asm` 与后端组合，继续保持默认关闭 |
| `tests/test_asm_peephole.py` | 扩充 | 增加规则回归、负例、固定点、标签、活跃性和 CLI 测试 |
| `benchmarks/bench_asm_peephole.py` | 修改 | 按有效指令统计，并记录迭代轮数和规则命中 |
| `docs/topics/13-窥孔优化器.md` | 同步 | 修正规则语义、实现状态和安全边界说明 |

### 3.2 分步实现计划

| 步骤 | 任务描述 | 预期产出 | 验证方式 |
|------|----------|----------|----------|
| 1 | 把现有缺陷固化为失败测试：两条 `mv`、行内标签、缺失分支目标、非法/超范围立即数和同形替换 | 当前风险可稳定复现 | 定向运行 `tests/test_asm_peephole.py`，确认新增用例先失败 |
| 2 | 复用 `_asm_parser.py`，定义寄存器别名、有效指令计数和保守屏障 | 非法、不确定或带边界的输入保持原样 | 解析往返、标签、注释、directive 和未知指令测试 |
| 3 | 修正五条现有规则并新增三条恒等运算规则 | 八条规则仅在表 2.2 的条件下生效 | 每条规则精确输出、精确命中数、反例及组合测试 |
| 4 | 增加块内保守活跃性、固定点判定和 `PeepholeStats` | 无空转计数，命中数与指令减少量口径一致 | 活跃/死亡、幂等、未收敛及统计测试 |
| 5 | 完成编译器后端保护、CLI 错误处理和模块导入兼容 | LLVM IR 不会进入优化器；CLI 无重复导入警告 | 编译器组合、CLI、同路径写入和退出码测试 |
| 6 | 完成全量回归、语义验证、基准测试并同步课题文档 | 接口、行为、性能数据和文档一致 | `make test`、RV32IM 端到端样例及基准对比 |

### 3.3 异常处理与边界条件

- [ ] 每条规则先精确检查操作数数量；整数解析支持带符号十进制及 `0x`、`0b`、`0o` 前缀，符号表达式和重定位表达式无法证明安全时保持原样。
- [ ] `addi+addi` 的两个输入立即数及合并结果都必须位于有符号 12 位范围；`li+addi` 按 RV32 的 32 位回绕语义计算并输出规范化常量。
- [ ] 带标签的指令不参与删除或合并；标签、directive、空行、纯注释、控制流、调用、返回和未知指令构成窗口屏障。
- [ ] 未修改的行使用 `raw` 原样输出；替换窗口中的注释按原顺序保留，删除指令上的注释转换为独立注释行。
- [ ] `x0`～`x31` 与 ABI 名称先规范化后比较，例如 `x5` 与 `t0` 视为同一寄存器。
- [ ] 活跃性检查先判断 `uses`：同一指令同时读取并写入候选寄存器时按读取处理；只有候选寄存器属于 `defs` 且不属于 `uses` 的纯覆写才能证明旧值死亡。函数调用、未知指令或块尾均按活跃性未知处理。
- [ ] 替换结果与输入相同时不计数；仅当第 50 轮仍发生变化时，将 `converged` 设为 `false` 并警告；第 50 轮无变化时仍视为正常收敛。
- [ ] 独立 CLI 使用 UTF-8；输入输出同路径时先写临时文件并在成功后原子替换，任何失败均不得破坏原文件。

---

## 4. 测试与验证方案

### 4.1 单元测试

- **测试文件位置**：`tests/test_asm_peephole.py`
- **当前基线**：现有 17 项定向测试能够通过，但两条 `mv` 测试只断言 `changes >= 0`，无法证明规则匹配或输出正确。
- **必要覆盖范围**：
  1. 八条目标规则的正常匹配、完整输出、精确变更次数、逐规则命中数和规则优先级。
  2. 为每条规则提供至少一个反例；不得继续使用 `changes >= 0` 这类无法证明规则生效的断言。
  3. 覆盖行内标签、注释、directive、控制流、寄存器别名、零寄存器、缺失操作数、十进制/十六进制立即数及 `-2048`、`2047`、单个输入越界和合并结果越界。
  4. 覆盖临时寄存器先读取、先纯覆写、读改写和到块尾仍未知四种活跃性路径。
  5. 覆盖无变化替换、规则冲突、重复运行幂等、50 轮上限、CLI 和编译器后端组合。

### 4.2 集成 / 基准测试

- **测试位置**：`tests/test_asm_peephole.py`、`benchmarks/bench_asm_peephole.py` 及项目现有端到端测试目录。
- **必要用例**：
  1. 连续常量运算：验证 `li+addi`、`addi+addi` 和多轮固定点。
  2. 分支与标签：验证恒真分支简化以及标签、注释完整保留。
  3. 寄存器搬运：验证回拷贝仅删除第二条、两条规则的零寄存器反例，以及临时寄存器“仍活跃”“块内纯覆写”“读改写”“块尾未知”四种路径。
  4. 编译器入口：验证原生 RV32IM 后端启用成功，LLVM 后端组合被拒绝或安全跳过。

基准测试使用 100～5000 条有效指令的目标规模，分别生成八条规则的可优化模式及不可优化反例，并加入 ScratchV 实际生成的 RV32IM 汇编。统计时排除标签、directive、空行和注释，记录耗时、有效指令减少量、逐规则命中次数和固定点轮数；实施前后使用相同环境和参数比较。

### 4.3 验收标准（Definition of Done）

- [ ] `python -m pytest tests/test_asm_peephole.py -q` 通过
- [ ] `make test` 全部通过且不引入回归
- [ ] 八条规则均通过完整输出、精确命中数和反例测试
- [ ] 至少三组完整 RV32IM 用例通过项目模拟器或 TinyFive 验证，优化前后的返回值、输出寄存器和可观察内存一致
- [ ] 优化器重复运行结果不变，非法、未知或超范围输入保持原样
- [ ] 编译器、独立 CLI 和 Python API 均通过验证；`--list-rules` 无需输入文件且不产生 `RuntimeWarning`
- [ ] 非 RV32IM 汇编后端不会进入窥孔优化器
- [ ] 基准测试按有效指令计数，且未触发 50 轮上限
- [ ] 代码注释及 `docs/topics/13-窥孔优化器.md` 与最终实现一致

---

## 5. 风险评估与依赖

| 风险项 | 影响程度 | 缓解措施 |
|--------|----------|----------|
| 公共解析器重建格式时丢失标签或注释布局 | 高 | 先增加解析往返和标签保真测试，再替换旧解析逻辑 |
| 操作数、立即数或活跃性判断错误，改变程序语义 | 高 | 精确校验操作数和范围；对控制流、调用和未知指令保守处理 |
| 对非 RV32IM 输出执行文本优化，破坏 LLVM IR 或其他后端结果 | 高 | 在 `main.py` 和 `compiler.py` 双重校验后端类型 |
| 规则顺序或固定点规则互相触发，造成规则不可达或无法收敛 | 中 | 增加组合与幂等测试，并保留 50 轮安全上限 |

- **目标架构**：本课题只处理 ScratchV 原生 RV32IM 汇编，不处理 LLVM IR 或 LLVM RV64FD 输出。
- **外部依赖**：不新增运行时第三方 Python 依赖；语义验证优先使用项目的 RV32 模拟器，也可使用现有 TinyFive 适配环境。
- **兼容性**：保持现有 Python API 和命令行参数；功能开关继续默认关闭。无法证明安全时保留原汇编。

---

## 6. 开发进度跟踪

| 阶段 | 计划完成日期 | 状态（待开始/进行中/已完成） |
|------|--------------|------------------------------|
| 当前实现与测试基线核验 | 2026-08-02 | 已完成 |
| 设计文档人工 Review | 待作者及课题组确认 | 进行中 |
| 测试先行与编码修复 | 待课题组确认 | 待开始 |
| 全量自测、语义验证与基准测试 | 待课题组确认 | 待开始 |
| 代码审查（PR） | 待课题组确认 | 待开始 |
| 合并主分支 | 待维护者审批 | 待开始 |

---

## 7. 附录

### 7.1 参考资料

- [`窥孔优化器设计文档.md`](./窥孔优化器设计文档.md)
- `scratchv/backend/asm_peephole.py`
- `scratchv/backend/_asm_parser.py`
- `scratchv/compiler.py`
- `scratchv/main.py`
- `tests/test_asm_peephole.py`
- `benchmarks/bench_asm_peephole.py`
- `docs/topics/13-窥孔优化器.md`

### 7.2 测试用例详情（RISC-V 汇编）

```asm
# 用例 1：连续常量运算
li t0, 5
addi t0, t0, 3
addi t0, t0, 2
# 预期结果：固定点优化后为 li t0, 10
```

```asm
# 用例 2：分支、标签与三条恒等运算删除
main:
    beq x0, x0, exit
exit:
    mv a0, a0
    addi a0, a0, 0
    slli a0, a0, 0
    ret
# 预期结果：分支改为 j exit，三条恒等运算被删除，标签和 ret 保留
```

```asm
# 用例 3：回拷贝消除
mv t0, t1
mv t1, t0
# 预期结果：只删除第二条，保留 mv t0, t1
```

```asm
# 用例 4：中间寄存器搬运，块内覆写证明旧值死亡
mv t0, a0
mv a1, t0
li t0, 0
ret
# 预期结果：前两条合并为 mv a1, a0，li t0, 0 和 ret 保留
# 反例：若第二条之后先读取 t0，或直接到达块尾，则不得执行该合并
```
