# Topic 18 后续任务

更新：2026-09-27。

## 已完成

- [x] 删除自建 CPU 计时器、静态参数表及提取脚本；调度收益仅由 LLVM MCA / SiFive E76 判定。历史报告使用 18.1.3。
- [x] 统一调度与报告的 LLVM 配置，保存规范化输入、原始 JSON、候选及最终周期。
- [x] 缺失工具、无效分析结果、部分解析及不完整时间线直接失败，不使用替代模型。
- [x] 报告明确峰值并行数、气泡和周期的定义；关键路径标为 N/A，说明 LLVM 目标限制。
- [x] 固定仓库 CNN，运行 standalone 主路径及 CompilerDriver greedy/linear 补充路径。
- [x] 保留语义验证、代码重编码、权重、寄存器、栈访存、CFG 活跃性和三组完整 CNN 执行检查。
- [x] 沿用现有 CI jobs，更新工具依赖与测试，移除参数表校验。
- [x] 更新文档和报告，保留原设计文档及 SPEC Review。
- [x] CI 保留 `ubuntu-latest`，报告记录实际 LLVM 版本；18.1.3 仅用于复现已有评测数值。

## 后续问题

- [ ] 关键路径：调查 LLVM 对该顺序执行目标可提供的依赖分析接口；不得将 TotalCycles 或自定权重替代关键路径。
- [ ] 数值精度：验证 Q16.16 乘法溢出范围及 Sigmoid 近似误差，与 ONNX 浮点参考比较。
- [ ] 动态性能：补充循环次数、分支频率和硬件或校准模拟器测量。
- [ ] 指令覆盖：修正 CompilerDriver 的 `max` 展开及非法 `slt` 操作数，再扩展可分析区域。

结果与复现：[验证报告](18-指令调度器Review迭代报告.md)、[实现说明](18-指令调度器代码说明.md)。
